[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ledger-candidate"
version = "1.4.0"
description = "A deliberately flawed release candidate used by Praval notebooks"
requires-python = ">=3.9"
